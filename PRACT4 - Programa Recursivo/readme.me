En la forma recursiva es mas legible y mas facil de entender y sus desventajas es sumamente ineficiente al no "recordar" cálculos previos, la función genera un árbol de llamadas redundantes que crece exponencialmente, cada llamada recursiva ocupa un lugar en la memoria
En la forma iterativa utiliza un bucle for para sumar los valores de forma secuencial,en este código, solo estamos usando dos variables (a y b) que se van sobrescribiendo. y sus desventajas necesita más variables de control. Si te equivocas en el orden de a, b = b, a + b, el resultado será basura, mientras que en el recursivo es más difícil equivocarse en la lógica.

